# The solver itself needs only numpy.
numpy>=1.24
# The three packages below are imported by the unmodified MIKASA-Base environment files (Passive T-maze imports
# matplotlib; the bsuite wrapper imports gymnasium and dm_env). They are not dependencies of the solver.
gymnasium>=0.29
dm-env>=1.6
matplotlib>=3.7
